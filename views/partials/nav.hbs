<nav class="nav h-100">
    <div class="row h-100 m-0">
        <div class="col-sm-9 p-0 nav_section">
            <div class="nav_top">
                <div class="nav_close cursor-pointer js-slidePanel" data-target=".nav">
                    <i class="ico ico-cross"></i>
                </div>
            </div>

            {{#each (theme code="navigation")}}
                {{> navBlock }}
            {{/each}}
        </div>
    </div>
</nav>
