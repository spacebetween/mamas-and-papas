<div class="slidePanel slidePanel-{{ifSetOrDefault align 'leftSide'}} vh-100">
    <div class="slidePanel_content vh-100 bg-white">
        <div class="slidePanel_top">
            <div class="slidePanel_close cursor-pointer js-slidePanel">
                <i class="ico ico-cross"></i>
            </div>
        </div>
        {{> (lookup . 'block') }}
    </div>
</div>
