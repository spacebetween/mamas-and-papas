<footer class="footer">
    <div class="container">
        <div class="row">
            <div class="col-md hidden-md-up">
                <h4 class="heading">Change Location</h4>
                <ul class="links">
                    <li>Lorem Ipsum</li>
                </ul>
            </div>
            {{#each (theme code="footer_links")}}
            <div class="col-md">
                <h4 class="heading">{{@key}}</h4>
                <ul class="links">
                    {{#each .}}
                        <li>{{.}}</li>
                    {{/each}}
                </ul>
            </div>
            {{/each}}
        </div>

        <div class="row">
            <div class="col social">
            {{#each (theme code="social_links")}}
                <i class="fa fa-{{.}}" aria-hidden="true"></i>
            {{/each}}
            </div>
        </div>
    </div>
</footer>